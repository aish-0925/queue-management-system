# Server
server.port=8080

# MongoDB (adjust as needed)
spring.data.mongodb.uri=mongodb://localhost:27017/smartqueue

# Websocket (optional)
spring.main.allow-bean-definition-overriding=true

# Logging
logging.level.org.springframework=INFO

